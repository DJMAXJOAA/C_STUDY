ctrl shift s 전체 저장
ctrl shift b 솔루션 빌드 ( 컴파일 없이 빌드만 진행 )
F5 디버깅 / 에러 수정 (논리버그 수정, 의심가는 부분에)
	왼쪽에 빨간색 점으로 라인을 고정 시키면 그 줄까지만 실행
	에러를 찾으면 디버깅 중지 shift F5
	System(“Pause”); 를 작성하면 디버깅시에 지나치지 않고 멈춤
F10 한줄씩 실행

명령 프롬프트 -> cd경로(C:\Users\User\Desktop\work\ConsoleApplication1\Debug)
Dir 치면 경로 안에 있는 파일들을  보여줌
실행시키고 싶은 파일 이름 치면 파일이 실행됨
(워드 게임 만들 때 응용할 것)
아스키코드로 이미지처럼, 구현 가능

#define _CRT_SECURE_NO_WARNINGS_

sdk의 헤더파일에 정의가 들어가있음
함수들 안

void (무엇을 반환할지 모름) - > return; 이나 사용x	
{ } 블럭의 시작
머리(head), 몸통(body)					//25p

%c 문자 (character)
%s 문자열 (string)
%d 숫자 (digit)
%f 실수 (float ) / %lf  (long float)

세미콜론이 나오기 전까지는 줄이 끝난 게 아니다.
세미콜론을 입력하면 엔터 해도 ㄱㅊ

\n 줄바꿈
\b 백스페이스(하나 지움)
// \b 백스페이스는 한 칸 전으로 가서 goot의 t 자리에 d로 덮어씌움
\a 알람소리 (윈도우 오류소리)
\r 맨앞으로 이동
// \r 백스페이스처럼 맨앞으로 가서 덮어씌운다





